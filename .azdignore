# azd packages only `.next/standalone` (see azure.yaml `dist`).
# Do NOT add `node_modules` here — the standalone server bundles a
# trimmed node_modules (including `next`) that is required at runtime.

# Build cache is not needed at runtime.
.next/cache
*.log
